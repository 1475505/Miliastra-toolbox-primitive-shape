# Shaper 使用指南

Shaper 是一个用基础图元拟合图片的工具：**填充模式**在区域内随机优化拟合（圆/椭圆、矩形、三角形），**轮廓模式**沿轮廓行走排列（圆/椭圆、矩形）。拟合结果可导出 GIA、Lua、JSON、CSS、SVG、PNG。

## 1. 快速开始

1.  **上传图片**: 在左侧面板点击或拖拽上传图片。建议使用背景清晰、轮廓分明的图片。
2.  **配置图元**: 在「图元」区域选择圆形和矩形的预设类型及颜色。
3.  **点击开始**: 直接点击底部的「开始处理」按钮，使用默认参数进行拟合。
4.  **查看结果**: 处理完成后会自动跳转到结果页，您可以查看拟合出的图元列表，并导出 GIA（超限/经典）、Lua、JSON、CSS、SVG 或 PNG 文件。

## 2. 参数说明

上传页按处理模式给出两组参数，先在上方切换模式：

**填充模式（默认）**

*   **图元数量**: 40–1200，也可手动输入到 3000。越多越细，但耗时也更高。
*   **输出尺寸**: 「按比例缩放」用倍率（0.2–4）缩放；「指定分辨率」直接填宽高（16–4096，可锁定等比），此时缩放按 1.0 处理。
*   **透明度**: 0–100%，控制导出图元的整体不透明度。
*   **PNG 模式**: 开启后保留 PNG 的透明背景；关闭时先铺白底再拟合。

**轮廓模式**

*   **图元大小 (Primitive Size)**:
    *   3–200，每个图元的基础尺寸。数值越小，生成的图元越密集、越小；数值越大，生成的图元越大。
    *   *提示*: 如果您的图片线条很细，建议调小此值。
*   **精度 (Precision)**:
    *   0–1，控制图元与轮廓的贴合程度。
    *   高精度要求图元必须紧贴轮廓，可能会减少生成的图元数量；低精度允许一定的偏差，可能会生成更多图元。
*   **间距 (Spacing)**:
    *   0.1–1.0，控制相邻图元之间的重叠程度。
    *   数值越小，图元越紧密（允许更多重叠）；数值越大，图元越稀疏。

## 3. 图元预设说明

在「图元」区域勾选要启用的图元类型——圆形、矩形、三角形（默认只启用圆形，可按需叠加）。圆形与矩形各自选择预设和颜色；三角形没有预设，固定为等边三角形。

### 圆形预设

| 预设 | 尺寸 | 说明 |
|------|------|------|
| 冒险币 1×1 | 1×1 | 默认冒险币规格 |
| 岩元素徽章 0.3×0.3 | 0.3×0.3 | 低负载，适合小物件 |
| 自定义 | 可调 | 自定义尺寸 |

### 矩形预设

| 预设 | 尺寸 | 说明 |
|------|------|------|
| 木质柱子 0.5×5 | 0.5×5 | 默认木质柱子规格 |
| 自定义 | 可调 | 自定义尺寸 |

**颜色选择**: 圆形和矩形分别有独立的颜色选择器，选中的颜色将应用于对应类型的图元。

### 关于岩元素徽章

岩元素徽章是特殊的低负载图元，适用于需要降低游戏内负载的场景。选择此预设时：
- 图元尺寸为 0.3×0.3 单位
- GIA 导出时自动应用特殊旋转配置（Z轴90°，Y轴+90°）

## 4. 原点设置 (Origin)

您可以控制坐标系的原点位置，这对于导出数据的坐标值非常重要：

*   **图像中心 (默认)**: 以图片的几何中心 (Width/2, Height/2) 为原点 (0,0)。
*   **左上角**: 以图片左上角为原点 (0,0)，X轴向右，Y轴向下。
*   **自定义坐标**: 手动输入原点在图片像素坐标系中的位置 (X, Y)。

在结果页面，您还可以通过**右键点击画布**临时更改显示的原点位置。

## 5. 结果交互

*   **缩放/漫游**: 暂不支持鼠标滚轮缩放，但会自动适配窗口大小。
*   **高亮**: 鼠标悬停在图元上可高亮显示，右侧面板会显示该图元的详细参数。
*   **导出**（结果页左侧「导出」区按用途分组，JSON / CSS / Lua 可点「复制」直接复制）:
    *   **GIA (超限模式)**: 导出可导入游戏的 .gia 文件，支持当前选择的图元预设。
    *   **GIA (经典模式)**: 导出带经典模式标记的 .gia 文件。
    *   **Lua**: 导出客户端绘制脚本，挂到客户端容器即可在游戏中绘制；见第 6 节。
    *   **JSON**: 导出包含所有图元坐标、尺寸、角度的结构化数据。
    *   **CSS**: 导出页面样式规则，适合前端项目集成；使用时需要配套 HTML 节点与 JavaScript 创建图元节点。
    *   **SVG**: 导出矢量图结果，适合直接查看或在设计工具中二次编辑。
    *   **PNG**: 导出当前视图的渲染图片。

## 6. Lua 客户端绘制脚本

结果页「导出 / Lua」导出的是一份可直接挂进游戏的**客户端绘制脚本**：每个图元实例化一个图片控件，矩形 / 圆形 / 三角形分别对应静态图片 `100001` / `100002` / `100003`，位置、旋转、颜色与透明度按拟合结果写入。脚本头部自带同一份使用说明。

使用前必填 `IMAGE_PREFAB_ID`：

1.  准备一个客户端**图片**控件，设为「仅存为模板」，关闭模板遮罩 / 羽化。
2.  把脚本里的 `IMAGE_PREFAB_ID = 0` 改成该控件的**控件模板索引 ID**（不是图片资产 ID）；只需填这一处，脚本会自动按图元类型切换图片。
3.  把脚本作为客户端脚本挂到一个**专用空客户端容器节点**——不要挂在已有界面的根节点上，脚本会设置该节点尺寸；进入运行预览后在 `OnStart` 绘制，不要移到 `OnInit`。`OnDestroy` 会自动清理，重复进入会先清理再重建。

可选参数：`BASE_SCALE`（整体缩放）、`OFFSET_X/Y`（平移，Y 向上）、`SKIP_BACKGROUND`（跳过背景图元）、`FIT_TO_CANVAS`（超出画布自动缩小）。每个图元创建一个控件，控件容量与真机显示请在运行预览和真机上确认。

命令行也可以直接导出：

```bash
python server.py --cli --input demo.png --export lua --output output.lua
```

## 7. GIA 模式转换

上传页面顶部的「GIA模式转换」标签页提供了独立的 GIA 文件转换功能：

- **超限模式转经典模式**：上传超限模式 .gia，写入经典模式标记后下载 `_classic.gia`
- **经典模式转超限模式**：上传经典模式 .gia，移除经典模式标记后下载 `_overlimit.gia`
- **素材组 GIA 转 Lua 绘制脚本**（超限模式）：把已有图片素材组直接转成 Lua 绘制脚本，可下载或复制；保留键鼠布局、颜色、透明度与层序，文本 / 嵌套组 / 动态图片需先转成单层图片

前两种转换仅修改文件头部的模式字段，不会重新生成素材内容。

## 常见问题

**Q: 为什么我设置了颜色和原点，结果里好像没生效？**
A: 请确保您使用的是最新版本的 Shaper。在旧版本中，颜色和自定义原点功能尚未实装。现在您可以直接在上传页面设置「原点类型」以及为图元指定颜色，处理结果将严格遵循您的设置。

**Q: 这里的 W 和 H 输入框有什么用？**
A: 目前算法采用自动拟合策略，W 和 H 输入框主要用于标记意图，实际生成的图元尺寸主要由「图元像素大小」滑块和图像轮廓特征决定。

**Q: 如何让结果坐标符合我的 CNC/加工设备坐标系？**
A: 使用「自定义原点」功能，输入您设备对应的零点在图片中的像素位置，导出的 JSON 数据中的 `relative_position` 即为相对于该原点的坐标。

**Q: 岩元素徽章预设有什么特殊之处？**
A: 岩元素徽章是游戏内的低负载图元。选择此预设后，导出的 GIA 文件将使用特殊的 type_id (20001285) 和旋转配置，确保图元在游戏内正确显示。
